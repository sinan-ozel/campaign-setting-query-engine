# Copy to .env and fill in values for local development.
# docker-compose.yaml picks up .env automatically.

# LLM endpoint — substituted into config/llm.yaml at graph-worker startup.
LLAMA_CPP_HOST=http://your-gpu-host:8080/v1
